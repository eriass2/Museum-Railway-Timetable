# Local Docker overrides (copy to .env and adjust).
# Docker Compose reads this file automatically.

# Host port for WordPress (default 8080). Change if you have a port conflict.
MRT_WP_PORT=8089

# Optional: full site URL (overrides MRT_WP_PORT). Must match the port above.
# MRT_DEV_SITE_URL=http://localhost:8081
