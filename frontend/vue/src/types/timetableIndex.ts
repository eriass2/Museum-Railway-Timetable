export type TimetableIndexItem = {
  url: string;
  label: string;
  meta: string;
  modifier: string;
  ariaHint: string;
};

export type TimetableIndexLabels = {
  intro: string;
  navAria: string;
};
